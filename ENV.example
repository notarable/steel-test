# SMTP settings for form submissions (/api/lead)
# If not set, form will work in "mock" mode (no email sent) and redirect to /spasibo/?mock=1

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=mailer@example.com
SMTP_PASS=change_me

# Optional
SMTP_FROM="SteelFasad Engineering <mailer@example.com>"
LEADS_TO_EMAIL=sales@example.com

